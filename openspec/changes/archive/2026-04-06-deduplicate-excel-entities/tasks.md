## 1. Implement deduplication logic

- [x] 1.1 In `anonymizer/gui.py`, update `_detection_thread` to use a `seen` set preventing duplicate `(text, entity_type)` from being added to the `rows` list.
- [x] 1.2 In `anonymizer/cli.py`, update the `anonymize detect` command to use a `seen` set preventing duplicate `(text, entity_type)` from being added to the `data_rows` list.

## 2. Testing and Validation

- [x] 2.1 Verify that the Excel generated by the GUI contains only unique entities.
- [x] 2.2 Verify that the Excel generated by the CLI contains only unique entities.
- [x] 2.3 Verify downstream anonymization still replaces all instances in the document correctly given the deduplicated Excel output.
